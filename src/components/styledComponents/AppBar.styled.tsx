import { AppBar } from "@mui/material";
import styled from "styled-components";

export const AppBarStyled = styled(AppBar) `
    height: 4rem;
    justify-self: start;
`

// export default function StyledComponents() {
//     return <AppBarStyled defaultValue={30} />;
//   }